# Build Chrome Extension

Run the chrome extension build script that:
1. Increments the version in package.json
2. Removes the existing engagekit-extension.zip file
3. Runs the build command
4. Creates a new zip file

Execute the build script:

```bash
./.claude/build-extension.sh
```